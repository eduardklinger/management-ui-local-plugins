{
  "id": "plugin-config",
  "name": "Root Config",
  "description": "Single root app config for .local-plugins (theme, enabledPlugins, URLs).",
  "version": "1.0.0",
  "category": "config",
  "extensionPoints": ["app:config"],
  "workspaceDependencies": {
    "@oc-mui/plugin-system": ">=1.0.0"
  }
}
