# Cyan Cat Rain Plugin

Built-in demo plugin for quick live walkthroughs.

- Route: `/cyan-cat-rain`
- Sidebar item: `Cyan Cat Rain`
- Purpose: show plugin development flow with an obvious visual interaction.

This plugin uses namespace `core` so it is loaded in development without additional `pluginNamespace` config changes.
